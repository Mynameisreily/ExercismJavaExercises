class PigLatinTranslator {

  import java.util.*;

  public String translate(String phrase) {

    //Splitting the string for each word (if there are multiple)
    String[] piggy = phrase.split();

    //Different arrays of specific things to look for in the strings
    String[] vowel = {"xr", "yt", "a", "e", "i", "o", "u"};
    String[] consSound = new String[""]


    //NOTE
    //Vowel Sound  -> "-ay" at the end
    //Consonant Qu ->
    //Consonant y  ->
    //Consonant    ->

    for(int i = 0; i < vowel.length(); i++) {
      if(phrase.contains())
    }
  }
}
